# Gui_Standalone Requirements
# Install with: pip install -r requirements2.txt

# ═══════════════════════════════════════════════════════════
# CORE (required for all tests)
# ═══════════════════════════════════════════════════════════

# GUI Framework
PyQt5>=5.15.0
PyQt5-sip==12.13.0

# Computer Vision
opencv-python>=4.5.0
numpy>=1.21.0

# Deep Learning (YOLO + Depth Estimation)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Object Detection (person detection, digit detection)
ultralytics>=8.0.0

# Image Processing
Pillow>=8.0.0

# Pose Estimation (Standing On One Leg, Seated Forward Bend)
mediapipe>=0.10.0

# Data Analysis (Walking Speed analysis)
pandas>=1.3.0

# Signal Processing (Walking Speed smoothing)
scipy>=1.7.0

# Plotting (optional, for Walking Speed charts)
matplotlib>=3.4.0

# ═══════════════════════════════════════════════════════════
# CAMERA-SPECIFIC (required for real-time Kinect tests)
# ═══════════════════════════════════════════════════════════

# Azure Kinect (FRT, SFB, TUG, Standing One Leg)
# Requires Azure Kinect SDK installed on system FIRST
pyk4a>=1.3.0

# ═══════════════════════════════════════════════════════════
# OPTIONAL (only needed for specific tests)
# ═══════════════════════════════════════════════════════════

# Kinect v2 — only needed for Grip Strength (digit detection) test
# Requires Kinect v2 SDK installed on system FIRST
# pip install pykinect2

# Depth Pro — only needed for Walking Speed depth estimation
# pip install depth_pro
